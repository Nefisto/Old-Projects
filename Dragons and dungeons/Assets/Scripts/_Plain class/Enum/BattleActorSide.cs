/// <summary>
/// Will decide which side is friendly and which isn't.
/// Also simply the modifiers (buffs\debuffs) to apply and temporary change it
/// </summary>
public enum BattleActorSide
{
    Player,
    Enemy
}